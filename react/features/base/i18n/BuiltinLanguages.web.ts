/**
 * Builtin languages for web platform.
 *
 * Web platform uses dynamic loading via HTTP backend (i18next-http-backend),
 * so no builtin languages need to be registered here. This file exists to
 * satisfy the dynamic import in i18next.ts.
 */
export {};
